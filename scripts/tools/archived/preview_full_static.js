#!/usr/bin/env node
// Archived: moved from scripts/preview_full_static.js
const { spawnSync, spawn } = require('child_process');
const fs = require('fs');
const path = require('path');

// ...existing code preserved in archive

console.log('This is an archived copy of preview_full_static.js');
